#!/usr/bin/python3
"""JSON = JavaScript Object Notation"""
import json


def to_json_string(obj):
    """dumps is used to convert a python object into a JSON string/
    dumps with a s = s = string`"""
    return json.dumps(obj)
